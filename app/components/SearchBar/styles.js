import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  input: {
    height: 50,
    margin: 12,
    borderWidth: 0,
    borderRadius: 5,
    padding: 10,
    backgroundColor: '#FFFFFF',
    fontSize: 20,
  },
});
